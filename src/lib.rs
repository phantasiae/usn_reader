mod usn_journal;
mod windows_string;
mod friendly_usn_record;
mod reason;
mod volume_handle;
mod reader;
mod usn_journal_record;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
