mod usn_journal;
mod windows_string;
mod volume_handle;
mod usn_journal_record;
mod usn_journal_record_iter;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
